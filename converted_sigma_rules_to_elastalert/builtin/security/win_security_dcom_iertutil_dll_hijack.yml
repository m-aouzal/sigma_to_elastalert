description: |
  Detects a threat actor creating a file named `iertutil.dll` in the `C:Program FilesInternet Explorer` directory over the network for a DCOM InternetExplorer DLL Hijack scenario.
name: DCOM InternetExplorer.Application Iertutil DLL Hijack - Security
index: "winlogbeat-*"
filter:
- query:
    query_string:
type: any
priority: 3
