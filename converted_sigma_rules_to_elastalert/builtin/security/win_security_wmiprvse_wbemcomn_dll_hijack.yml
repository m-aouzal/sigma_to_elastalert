description: |
  Detects a threat actor creating a file named `wbemcomn.dll` in the `C:WindowsSystem32wbem` directory over the network for a WMI DLL Hijack scenario.
name: T1047 Wmiprvse Wbemcomn DLL Hijack
index: "winlogbeat-*"
filter:
- query:
    query_string:
type: any
priority: 3
