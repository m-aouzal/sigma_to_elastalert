description: |
  DLLs that are specified in the AppInit_DLLs value in the Registry key HKLMSoftwareMicrosoftWindows NTCurrentVersionWindows are loaded by user32.dll into every process that loads user32.dll
name: New DLL Added to AppInit_DLLs Registry Key
index: "winlogbeat-*"
filter:
- query:
    query_string:
type: any
priority: 2
