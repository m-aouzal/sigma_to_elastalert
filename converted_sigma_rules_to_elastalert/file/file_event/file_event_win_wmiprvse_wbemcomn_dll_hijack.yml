description: |
  Detects a threat actor creating a file named `wbemcomn.dll` in the `C:WindowsSystem32wbem` directory over the network and loading it for a WMI DLL Hijack scenario.
name: Wmiprvse Wbemcomn DLL Hijack - File
index: "winlogbeat-*"
filter:
- query:
    query_string:
type: any
priority: 4
